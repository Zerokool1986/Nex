# NEX Identity & Key Hierarchy

Identity is anchored in Ed25519 keypairs. Sub-keys, device keys, and ephemeral session keys are derived hierarchically or negotiated via ECDH for forward-secret wire communication.
